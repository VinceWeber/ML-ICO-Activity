#Module to store function to folllow the script

def myprint(message, index, total_index):
    my_text='**************   ' + message + ' Batch ' + str(index) + '/' + str(total_index) + '**************'
    return my_text


def tick_time(message, previous_tick):

    return my_text